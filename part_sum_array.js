let arr = [1, 2, 3, 4, 5];
let newArr = [];

function getSums(arr) {
  let result = arr.reduce(function(sum, current) {
    newArr.push(result)
    return sum + current; console.log(sum);
    }, 1);
}

getSums(newArr);
// console.log(partSum);
